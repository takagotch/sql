#
SELECT DISTINCT shohin_id, cost_price
-- text
  FROM Product;

SELECT DISTINCT shohin_id, cost_price
/*
 */
  FROM Product;

#
--
SELECT DISTINCT product_id, cost_price
  FROM Product;
/*
 */
SELECT DISTINCT product_id, cost_price
  FROM Product;

#
SELECT product_name, product_category
 WHERE product_category = 'ticket1'
  FROM Product;

SELECT product_name
  FROM Product
 WHERE product_category = 'ticket2';

#
SELECT product_name, product_category
  FROM Product
 WHERE product_category = 'ticket1';

#
SELECT DISTINCT product_categroy, date
  FROM Product;

#
SELECT DISTINCT product_category
  FROM Product;

#
SELECT DISTINCT cost_price
  FROM Product;

#
SELECT 'ticket' AS , 38 AS , '' AS ,
        product_id, product_name
  FROM Product;

#
SELECT product_id   AS "",
       product_name AS "",
       cost_price   AS ""
  FROM Product;

#
SELECT product_id    AS   id,
       product_name  AS name,
       product_price AS price
  FROM Product;

#
SELECT *
  FROM Product;

#
SELECT product_id, product_name, product_category, sale_price,
       cost_price, date
  FROM Product;

#
SELECT *
  FROM Product;

#
SELECT product_id, product_name, cost_price
  FROM Product;


